[pytest]
# 用例搜索路径与命名规则
testpaths = testcases
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# 默认参数：-v 详细输出；-s 显示 print；生成 html 报告
addopts = -v -s --html=reports/report.html --self-contained-html
# 自定义标记
markers =
    smoke: 冒烟用例（核心主流程）
    regression: 回归用例
